{
  "name": "cinema-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
